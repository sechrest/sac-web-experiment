<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ title }} | SAC Web Experiment</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav>
  {% for item in navigation %}
    <a href="{{ item.url }}">{{ item.title }}</a>
  {% endfor %}
  </nav>

  <main>
    {{ content | safe }}
  </main>
</body>
</html>
